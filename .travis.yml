language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-java' ]; then cd .. && eval "mv $currentfolder generator-java" && cd generator-java; fi

